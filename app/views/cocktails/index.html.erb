<%= render 'layouts/_shared/banner' %>
<div class="container">
  <div class="cards">
    <% @cocktails.each do |cocktail| %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path cocktail.photo.key %>)">
        <%= link_to cocktail.name, cocktail_path(cocktail), class: "btn btn-outline-light" %>
      </div>
    <% end %>
  </div>
</div>
